# Example environment variables for chatbot_backend
# No OneDrive variables are required anymore.

# Django settings
# DEBUG=true
# SECRET_KEY=replace-with-a-secure-random-string

# The application writes notes to: C:\Nilesh_TATA\Prescription
# Ensure the running user has permission to create and write files in that directory.
